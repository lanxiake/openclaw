# OpenClaw AI 个人助理平台 - 测试计划

> 版本: 1.0 | 创建日期: 2026-02-08 | 状态: 执行中
>
> 本文档定义完整的测试策略、执行顺序和验收标准

---

## 修订记录

| 版本 | 日期 | 修订内容 |
|------|------|----------|
| 1.0 | 2026-02-08 | 初始版本 |
| 1.1 | 2026-02-08 | 完成密码工具和JWT工具测试 |
| 1.2 | 2026-02-08 | 创建用户仓库测试，发现Mock数据库问题 |

---

## 测试进度统计

**最后更新**: 2026-02-08

| 指标 | 数值 |
|------|------|
| 已完成测试文件 | 2 / 10 |
| 已通过测试用例 | 30 / 预计100+ |
| 当前代码覆盖率 | < 1% (仅测试了工具模块) |
| P0 优先级完成度 | 28.6% (2/7) |

**已完成模块**:
- ✅ 密码工具 (password.test.ts) - 12个测试用例全部通过
- ✅ JWT工具 (jwt.test.ts) - 18个测试用例全部通过

**已创建但需修复**:
- ⚠️ 用户仓库测试 (users.test.ts) - 22个测试用例已创建，但Mock数据库需要修复

**技术问题**:
- Mock数据库的构建器未实现Promise接口，导致仓库测试失败
- 需要修复Mock数据库或使用真实数据库进行测试

**下一步**:
- 修复Mock数据库实现
- 或者跳过仓库层测试，直接测试服务层（使用Mock仓库）

---

## 1. 测试概述

### 1.1 测试目标

| 目标 | 描述 |
|------|------|
| 功能正确性 | 验证所有业务逻辑按设计文档实现 |
| 代码覆盖率 | 达到 70% 以上的行覆盖率 |
| 回归保护 | 防止后续修改引入新 Bug |
| 调试友好 | 通过详细日志快速定位问题 |

### 1.2 测试范围

| 模块 | 测试类型 | 优先级 | 状态 |
|------|----------|--------|------|
| 密码工具 (utils/password) | 单元测试 | P0 | ✅ 已完成 (12/12 通过) |
| JWT 工具 (auth/jwt) | 单元测试 | P0 | ✅ 已完成 (18/18 通过) |
| 用户仓库 (repositories/users) | 单元测试 | P0 | ⏳ 待测试 |
| 用户认证服务 (auth-service) | 单元测试 | P0 | ⏳ 待测试 |
| 管理员认证服务 (admin-auth-service) | 单元测试 | P0 | ⏳ 待测试 |
| 订阅服务 (subscription/service) | 单元测试 | P0 | ⏳ 待测试 |
| 技能服务 (skill-service) | 单元测试 | P0 | ⏳ 待测试 |
| ID 生成工具 (utils/id) | 单元测试 | P1 | ⏳ 待测试 |
| Gateway RPC 方法 | 集成测试 | P1 | ⏳ 待测试 |
| 前后端 E2E | E2E 测试 | P2 | ⏳ 待测试 |

### 1.3 测试框架

| 工具 | 用途 |
|------|------|
| Vitest | 测试运行器 |
| V8 Coverage | 代码覆盖率 |
| Mock Database | 内存数据库模拟 |
| vi.fn() / vi.spyOn() | 函数 Mock |

---

## 2. 测试环境配置

### 2.1 环境变量

```bash
# 测试环境变量 (.env.test)
NODE_ENV=test
JWT_SECRET=test-jwt-secret-at-least-32-characters-long
DATABASE_URL=mock://localhost/test
LOG_LEVEL=debug
```

### 2.2 Mock 数据库使用

项目已提供 `src/db/mock-connection.ts`，支持内存数据库模拟：

```typescript
import {
  enableMockDatabase,
  disableMockDatabase,
  clearMockDatabase,
  seedMockTable,
  getMockDatabase,
} from '../../db/mock-connection.js';

// 测试套件设置
beforeAll(() => {
  enableMockDatabase();
});

afterAll(() => {
  disableMockDatabase();
});

beforeEach(() => {
  clearMockDatabase();
});
```

### 2.3 日志配置

测试时启用详细日志以便调试：

```typescript
// 在测试文件顶部
import { getLogger } from '../../logging/logger.js';

const logger = getLogger();
logger.level = 'debug';
```

---

## 3. 测试执行顺序

### 3.1 阶段划分

```
阶段 1: 基础工具测试 (无依赖)
├── utils/password.test.ts     - 密码哈希/验证
├── utils/id.test.ts           - ID 生成
└── auth/jwt.test.ts           - JWT 生成/验证

阶段 2: 数据访问层测试 (依赖 Mock DB)
├── repositories/users.test.ts         - 用户仓库
├── repositories/admins.test.ts        - 管理员仓库
└── repositories/subscriptions.test.ts - 订阅仓库

阶段 3: 业务服务层测试 (依赖仓库层)
├── auth/auth-service.test.ts              - 用户认证
├── admin-auth/admin-auth-service.test.ts  - 管理员认证
├── subscription/service.test.ts           - 订阅管理
└── skills/skill-service.test.ts           - 技能管理

阶段 4: Gateway RPC 方法测试 (集成测试)
├── server-methods/admin-auth.test.ts
├── server-methods/admin-users.test.ts
└── server-methods/assistant-auth.test.ts

阶段 5: E2E 测试
├── e2e/user-registration.e2e.test.ts
├── e2e/admin-login.e2e.test.ts
└── e2e/skill-store.e2e.test.ts
```

### 3.2 执行命令

```bash
# 运行所有单元测试
pnpm test

# 运行特定模块测试
pnpm test src/assistant/auth/

# 运行带覆盖率
pnpm test:coverage

# 监视模式
pnpm test:watch
```

---

## 4. 日志检查点设计

### 4.1 日志级别规范

| 级别 | 用途 | 示例 |
|------|------|------|
| DEBUG | 详细调试信息 | 函数入参、中间状态 |
| INFO | 关键业务事件 | 用户登录成功、订阅创建 |
| WARN | 潜在问题 | 登录失败、配额接近上限 |
| ERROR | 错误和异常 | 数据库连接失败、Token 验证失败 |

### 4.2 日志格式

```typescript
// 标准日志格式
logger.info("[模块名] 操作描述", {
  // 关键参数
  userId: "xxx",
  action: "login",
  // 结果
  success: true,
  // 耗时 (可选)
  durationMs: 123,
});
```

### 4.3 测试中的日志验证

```typescript
import { vi } from 'vitest';

// Mock logger
const mockLogger = {
  debug: vi.fn(),
  info: vi.fn(),
  warn: vi.fn(),
  error: vi.fn(),
};

// 验证日志调用
expect(mockLogger.info).toHaveBeenCalledWith(
  expect.stringContaining('[auth]'),
  expect.objectContaining({ userId: expect.any(String) })
);
```

---

## 5. Mock 策略

### 5.1 数据库 Mock

使用项目内置的 `mock-connection.ts`：

```typescript
import { getMockDatabase, seedMockTable } from '../../db/mock-connection.js';

// 预置测试数据
seedMockTable('users', [
  {
    id: 'test-user-1',
    phone: '+8613800138000',
    email: 'test@example.com',
    passwordHash: '$scrypt$...',
    isActive: true,
  },
]);
```

### 5.2 外部服务 Mock

```typescript
// Mock 验证码服务
vi.mock('../../services/sms-service.js', () => ({
  sendVerificationCode: vi.fn().mockResolvedValue({ success: true }),
}));

// Mock 支付服务
vi.mock('../../assistant/payment/service.js', () => ({
  createPayment: vi.fn().mockResolvedValue({ orderId: 'mock-order-1' }),
}));
```

### 5.3 时间 Mock

```typescript
import { vi, beforeEach, afterEach } from 'vitest';

beforeEach(() => {
  vi.useFakeTimers();
  vi.setSystemTime(new Date('2026-02-08T10:00:00Z'));
});

afterEach(() => {
  vi.useRealTimers();
});
```

---

## 6. 覆盖率目标

### 6.1 整体目标

| 指标 | 目标 | 当前 |
|------|------|------|
| 行覆盖率 (Lines) | ≥ 70% | - |
| 函数覆盖率 (Functions) | ≥ 70% | - |
| 分支覆盖率 (Branches) | ≥ 55% | - |
| 语句覆盖率 (Statements) | ≥ 70% | - |

### 6.2 模块级目标

| 模块 | 行覆盖率目标 |
|------|-------------|
| src/assistant/auth/ | ≥ 80% |
| src/assistant/admin-auth/ | ≥ 80% |
| src/assistant/subscription/ | ≥ 75% |
| src/assistant/skills/ | ≥ 75% |
| src/db/repositories/ | ≥ 80% |
| src/db/utils/ | ≥ 90% |

---

## 7. 测试进度跟踪

### 7.1 进度表

| 模块 | 测试文件 | 用例数 | 通过 | 失败 | 状态 |
|------|----------|--------|------|------|------|
| utils/password | password.test.ts | - | - | - | ⏳ |
| utils/id | id.test.ts | - | - | - | ⏳ |
| auth/jwt | jwt.test.ts | - | - | - | ⏳ |
| repositories/users | users.test.ts | - | - | - | ⏳ |
| auth/auth-service | auth-service.test.ts | - | - | - | ⏳ |
| admin-auth | admin-auth-service.test.ts | - | - | - | ⏳ |
| subscription | service.test.ts | - | - | - | ⏳ |
| skills | skill-service.test.ts | - | - | - | ⏳ |

### 7.2 状态说明

- ⏳ 待测试
- 🔄 进行中
- ✅ 通过
- ❌ 失败
- ⏸️ 阻塞

---

## 8. 问题跟踪

### 8.1 已知问题

| ID | 模块 | 问题描述 | 状态 |
|----|------|----------|------|
| - | - | - | - |

### 8.2 测试阻塞项

| ID | 阻塞原因 | 影响模块 | 解决方案 |
|----|----------|----------|----------|
| - | - | - | - |

---

## 9. 验收标准

### 9.1 单元测试验收

- [ ] 所有 P0 模块测试通过
- [ ] 行覆盖率 ≥ 70%
- [ ] 无跳过的测试用例
- [ ] 测试执行时间 < 60 秒

### 9.2 集成测试验收

- [ ] Gateway RPC 方法测试通过
- [ ] 认证流程端到端验证
- [ ] 订阅流程端到端验证

### 9.3 文档验收

- [ ] 测试用例文档完整
- [ ] 日志检查点文档完整
- [ ] 问题跟踪记录完整

---

*— 文档结束 —*

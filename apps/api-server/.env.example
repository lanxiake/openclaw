# App Server 环境变量配置

# 服务端口
API_SERVER_PORT=3000
API_SERVER_HOST=0.0.0.0

# 运行环境
NODE_ENV=development

# 数据库
DATABASE_URL=postgresql://localhost:5432/openclaw

# JWT 密钥 (必需，至少 32 字符)
JWT_SECRET=Oc@2026!JwtSecret#32CharMinimumRequired

# 管理员 JWT 密钥 (可选，默认使用 JWT_SECRET)
# ADMIN_JWT_SECRET=

# CORS 允许的来源 (逗号分隔)
CORS_ORIGINS=http://localhost:5173,http://localhost:5174

# API 限流：每分钟最大请求数
RATE_LIMIT_MAX=100

# 日志级别
LOG_LEVEL=debug

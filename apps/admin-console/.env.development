# 开发环境配置

# 启用模拟认证 (无需后端即可测试 UI)
# 设置为 true 时使用 admin/admin 登录
# 设置为 false 时调用 Gateway RPC 登录
VITE_MOCK_AUTH=false

# Gateway WebSocket 地址 (开发环境)
VITE_GATEWAY_WS_URL=ws://localhost:19001

# Gateway 认证 Token (用于开发环境绕过设备配对)
VITE_GATEWAY_AUTH_TOKEN=f3f422b4a5b046a599381233fb3449ae

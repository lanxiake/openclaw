---
summary: "CLI reference for `mtbot sessions` (list stored sessions + usage)"
read_when:
  - You want to list stored sessions and see recent activity
---

# `mtbot sessions`

List stored conversation sessions.

```bash
mtbot sessions
mtbot sessions --active 120
mtbot sessions --json
```

# 开发环境配置 - 多租户架构

# 启用模拟认证 (无需后端即可测试 UI)
# 设置为 true 时使用 admin/Admin@123456 登录
# 设置为 false 时调用 API Server REST 登录
VITE_MOCK_AUTH=false

# API Server 地址 (REST API)
VITE_API_SERVER_URL=http://localhost:3000

# Gateway WebSocket 地址 (实时控制)
VITE_GATEWAY_WS_URL=ws://localhost:18789

# Gateway 认证 Token (用于开发环境)
VITE_GATEWAY_AUTH_TOKEN=f3f422b4a5b046a599381233fb3449ae
